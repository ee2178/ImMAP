{
    "_comment": "Same sweep, denoising task. Nothing here names the task -- it comes from each run's own config.json, which routes to the adapter in evaluation/tasks.py.",
    "runs": "trained_nets/bsd432",
    "out": "results/denoiser.csv",
    "metrics": ["psnr", "ssim", "nrmse", "lpips"],
    "sigmas": [0.02, 0.05, 0.098],
    "seed": 1234
}
